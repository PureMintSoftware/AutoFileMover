# AutoFileMover
An automated file mover (or copier) that monitors for the presence of a file in a folder, and moves it to another folder when it arrives, every X seconds or minutes, with a sound notification if required.
